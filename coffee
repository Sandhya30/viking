#include<stdio.h>
#include<math.h>
int main()
{
  int X,P,count=0;
  scanf("%d %d",&X.&P);
  while(X<=10000 &&P<=100)
  {
    while(X!=0)
    {
      X=X-(p*100/X);
      count++;
      }
      printf("%d",count);
     }
    }
      
